const name = process.argv[2] || "World"
console.log(`Hello, ${name}`)

// to run :   node <filename> <argument>

// process -> global variable made available in node ( like window in browser )
// window and document not available in node - only browser

